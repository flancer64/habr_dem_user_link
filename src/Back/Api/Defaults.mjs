/**
 * Plugin constants (hardcoded configuration) for backend code.
 */
export default class Fl64_Dem_User_Link_Back_Api_Defaults {

    NAME = '@flancer64/habr_dem_user_link';

    constructor() {
        Object.freeze(this);
    }
}
